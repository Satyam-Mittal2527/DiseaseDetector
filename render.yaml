services:
  - type: web
    name: your-service-name  # Change this
    runtime: python
    pythonVersion: "3.10.13"  # Enforces Python 3.10
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py  # Adjust to your starter file
